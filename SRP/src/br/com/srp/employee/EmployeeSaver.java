package br.com.srp.employee;

// Classe responsável apenas por persistir os dados (ex: usada pelo banco de dados)
public class EmployeeSaver {

    public void saveEmployee(EmployeeData employeeData) {
        // Implementação da lógica para salvar no banco de dados
        System.out.println("Salvando dados do funcionário no sistema...");
    }
}
